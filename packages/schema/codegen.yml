overwrite: true
schema: ./schema.graphql
generates:
  types.generated.ts:
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-enum-array
    config:
      enumsAsTypes: true
      scalars:
        ISO8601DateTime: string
